%section.container
  .row
    .col-sm-10.col-sm-offset-1
      %article
        %header
          %h2 Owner Admin Panel
          %hr
          
        %br    
        %table.table
          %thead
            %tr 
              %h4 My Businesses
            %tr
              %th(width="20%") Name
              %th(width="20%") City, State
              %th(width="40%") URL
              %th(width="20%") Action
          %tbody
            -@verified_businesses.each do |business|
              %tr              
                %td.extra-padding
                  = business.name
                %td.extra-padding
                  = business.city + ", " + business.state
                %td.extra-padding 
                  = link_to fix_url(business.url) if business.url
                %td.extra-padding 
                  = link_to edit_owner_business_path(id: business.id) do
                    %i.glyphicon.glyphicon-pencil
                    Edit